# Session — 2026-04-09

## Daily Goal
- Goal: Describe every Fe v26+ syntactic construct from memory and explain what each effect (`store`, `mut store`, `ctx`, `call`, `log`, `evm`) controls — no gaps
- Status: 🔄 IN PROGRESS

## Topic
Fe v26+ Complete Syntax — every construct, every effect, no gaps

## Concepts to Cover
1. Contract declaration + contract-level `uses`
2. Storage: struct, `mut store`, `StorageMap` access (`.get`/`.set`), tuple keys
3. Messages (`msg`) + selectors (`#[selector = sol("...")]`)
4. Constructor (`init`) + its `uses` clause
5. Message handlers (`recv`) + per-variant `uses`
6. Effects system — all 6 effects: `store`, `mut store`, `ctx: Ctx`, `call: mut Call`, `log: mut Log`, `evm: mut Evm`
7. Events (`#[event]` structs + `log.emit`)
8. Enums (`pub enum`)
9. Free functions + `uses` clauses
10. Special attributes: `#[payable]`, `#[indexed]`
11. Cross-contract calls: `call.call()` vs `call.static()`
12. Testing: `#[test]`, `evm.create2`, `evm.call`, `Address { inner }`

## Theory Notes

(to be filled during session)

## Practice Log

(to be filled during session)

## Anki Cards

(generated after theory gate cleared)

## Session Notes

(compiled at end of session)